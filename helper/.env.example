# Copy this file to .env before running the Helper in production. MUST
# match the frontend's config/app-config.js helperAuthToken value
# exactly.

HELPER_PORT=5175
HELPER_AUTH_TOKEN=change-this-to-a-real-random-token
HELPER_ALLOWED_ORIGINS=http://localhost:4000,http://127.0.0.1:4000
